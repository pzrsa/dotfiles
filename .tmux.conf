set-option -g prefix C-a

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Set default term to xterm
set-option -g default-terminal "screen-256color"

# Fix vim in tmux delay when pressing <Esc>
set -s escape-time 0

